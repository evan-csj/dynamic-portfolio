import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
    colors: {
        light: {
            navy: '#10316B',
            blue: '#0B409C',
            yellow: '#FFCE63',
            white: '#F2F7FF',
        },
    },
});

export default theme;